# Copy to .env (gitignored — never commit real values) and fill in.
# .mcp.json's ${VAR} placeholders read from these. Claude Code does NOT
# auto-load .env — before launching `claude` in this workspace, run:
#   source .env
# or export the two vars in your shell profile.

POSTMAN_API_KEY=
SAP_API_HUB_KEY=
